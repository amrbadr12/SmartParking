<resources>

    <!-- Base application theme. -->

    <style name="SettingsTheme" parent="Theme.AppCompat.NoActionBar">
        <item name="preferenceTheme">@style/PreferenceThemeOverlay</item>
    </style>
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="preferenceTheme">@style/PreferenceThemeOverlay</item>
    </style>

    <style name="myTheme" parent="@android:style/Theme.Holo.Light">
        <item name="android:actionBarStyle">@style/myTheme.ActionBar</item>
        <item name="android:actionMenuTextColor">@color/colorText</item>
    </style>

    <style name="myTheme.ActionBar" parent="@android:style/Widget.Holo.Light.ActionBar">
        <item name="android:titleTextStyle">@style/myTheme.ActionBar.Text</item>
    </style>

    <style name="myTheme.ActionBar.Text" parent="@android:style/TextAppearance">
        <item name="android:textColor">@color/colorText</item>
    </style>

    <!-- Firebase theme -->
    <style name="AuthUITheme" parent="FirebaseUI">
        <item name="colorPrimary">@color/tealBlue</item>
        <item name="colorPrimaryDark">@color/darkSlateGrey</item>
        <item name="colorAccent">@color/lightBlue</item>
        <item name="colorControlNormal">@color/colorRed</item>
        <item name="android:windowBackground">@color/colorJetBlack</item>
        <item name="colorButtonNormal">@color/colorJetBlack</item>
    </style>
    material_green_600
    material_green_700

</resources>
